---
import type { Project } from "../../data/projects";
import Icon from "../Icon.astro";

interface Props {
    project: Project;
}

const { project } = Astro.props;
---

<section class="px-6 sm:px-8 pt-16 pb-12">
    <div class="max-w-4xl mx-auto">
        <div class="flex items-center gap-3 mb-8">
            <a
                href="/#work"
                class="text-sm text-muted hover:text-ink transition-colors duration-150"
            >
                ← Work
            </a>
            <span class="text-border">/</span>
            <span class="text-sm text-muted">
                {project.title}
            </span>
        </div>

        <div class="flex items-center gap-3 mb-5">
            <span class="text-sm font-medium text-accent">
                {project.type}
            </span>
            <span class="w-1 h-1 rounded-full bg-border"></span>
            <span class="text-sm text-muted">
                {project.year}
            </span>
        </div>

        <h1 class="font-sans font-extrabold text-[clamp(2.2rem,6vw,3.5rem)] leading-none tracking-[-0.025em] text-ink mb-6">
            {project.title}
        </h1>

        <p class="text-[15px] text-muted leading-relaxed max-w-xl mb-10">
            {project.tagline}
        </p>

        <div class="flex items-center gap-3">
            {
                project.live && (
                    <a
                        href={project.live}
                        target="_blank"
                        rel="noopener noreferrer"
                        class="inline-flex items-center gap-2 bg-ink text-white text-sm font-medium rounded-full px-6 py-2.5 hover:bg-ink/85 transition-colors duration-150"
                    >
                        <span class="relative flex h-1.5 w-1.5">
                            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-good opacity-75" />
                            <span class="relative inline-flex rounded-full h-1.5 w-1.5 bg-good" />
                        </span>
                        Live site
                        <Icon name="arrow-up-right" class="w-3.5 h-3.5" />
                    </a>
                )
            }

            {
                project.github && (
                    <a
                        href={project.github}
                        target="_blank"
                        rel="noopener noreferrer"
                        class="inline-flex items-center gap-2 text-ink text-sm font-medium border border-border rounded-full px-6 py-2.5 hover:border-ink/30 transition-colors duration-150"
                    >
                        <Icon name="github" class="w-3.5 h-3.5" />
                        GitHub
                    </a>
                )
            }
        </div>
    </div>
</section>
